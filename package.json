{
  "name": "monorepowered",
  "version": "1.0.0",
  "description": "A monorepo, batteries included",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "all:build": "pnpm run --parallel -r build",
    "all:dev": "pnpm run --parallel -r dev"
  },
  "keywords": [],
  "author": "Benjamin Decoster",
  "license": "ISC"
}
